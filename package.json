{
  "name": "erc20s",
  "version": "1.0.0",
  "description": "cheap erc20 deployment",
  "main": "index.js",
  "scripts": {
    "compile": "oz compile --optimizer on --optimizer-runs 999",
    "coverage": "scripts/coverage.js",
    "lint": "solhint --max-warnings 0 \"contracts/**/*.sol\"",
    "test": "mocha --exit --recursive --timeout 10000",
    "clean": "rimraf build"
  },
  "author": "Hadrien Croubois (@Amxx)",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^3.3.0"
  },
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/test-environment": "^0.1.8",
    "@openzeppelin/test-helpers": "^0.5.9",
    "chai": "^4.2.0",
    "ganache-cli": "^6.12.1",
    "mocha": "^8.2.1",
    "rimraf": "^3.0.2",
    "solhint": "^3.3.2",
    "solidity-coverage": "^0.7.13"
  }
}
